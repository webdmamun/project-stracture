import React from "react";

const ProductCard = () => {
  return (
    <div style={{ margin: "20px", padding: "20px", backgroundColor: "yellow" }}>
      ProductCard
    </div>
  );
};

export default ProductCard;
